return {
    TSmaxFileSize = 10,
    servers = require('config.servers'),
    session = {
        autosave = {
            enabled = true,
            interval = 290,
            notify = true,
        },
    }
}
